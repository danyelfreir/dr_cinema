import React from 'react';
import { View, Text } from 'react-native';
import Modal from '../../modal';
import styles from './styles';

const AboutCinemaModal = ({ isOpen, closeModal, description }) => {
  // implement? Annars verður Cinema view mjög skrítið

  return <></>;
};

export default AboutCinemaModal;
