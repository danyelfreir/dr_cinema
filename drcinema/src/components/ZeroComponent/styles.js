import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    // flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: 'red',
    // left: 100,
  },
  message: {
    fontSize: 18,
    fontWeight: 'bold',
    color: 'indianred',
  },
});
