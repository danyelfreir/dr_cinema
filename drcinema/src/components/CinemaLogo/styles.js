import { StyleSheet } from 'react-native';
import colors from '../../styles';

export default StyleSheet.create({
  image: {
    width: '100%',
    height: 100,
  },
  container: {
    alignItems: 'center',
  },
  text: {
    color: colors.white,
  },
});
