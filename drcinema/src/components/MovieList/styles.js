import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'dodgerblue',
  },
  header: {
    fontSize: 24,
    fontWeight: 'bold',
    color: 'white',
    left: 20,
  },
});
