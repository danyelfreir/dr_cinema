import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    backgroundColor: 'dodgerblue',
    borderRadius: 30,
    padding: 8,
  },
  icon: {
    fontSize: 20,
    color: 'white',
  },
});
